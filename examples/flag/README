flag-1.ss: flag designer with stars, stripes, circles.

flag-1-scoped.ss: flag-1, using scope-struct to reduce the amount of
adaptor functions written to hook to the global state.


flag-2-scoped.ss: blinking outline of the current shape shown, as well
as a cursor to see the current position.

flag-3-scoped.ss: adding undo.

flag-4-scoped.ss: adding redo.

